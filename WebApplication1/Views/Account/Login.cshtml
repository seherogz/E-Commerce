@using WebApplication1.ViewModels;
@model LoginVm
@{
    ViewData["Title"] = "Log-in";
}

<div class="row">

    @if (TempData["Error"] != null)
    {
        <div class="col-md-12 alert alert-danger text-center">
            <span><b>Üzgünüz.</b> - @TempData["Error"]</span>
        </div>

    }
    <div class="col-md-8 offset-3">
        <p>
            <h4>Lütfen gerekli bilgileri doldurunuz</h4>
        </p> 

        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Login" >
                    <div asp-validation-summary="ModelOnly" class="text-danger"> </div>

                    <div class="form-group">
                        <label asp-for="EmailAdress" class="control-label"></label>
                        <input asp-for = "EmailAdress" class="form-control"/>
                        <span asp-validation-for="EmailAdress" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>

                    </div>

                    <div class="form-group mt-2">
                        <input type="submit" class="btn btn-outline-success float-lg-end" value="Login" />
                        <a class="btn btn-outline-secondary" asp-controller="Movies" asp-action="Index">Vazgeç</a>
                    </div>

                </form>

            </div>

        </div>
    </div>

</div>
